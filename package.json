{
  "name": "fast-tracker-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for Fast Tracker and related applications",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "start": "lerna run start --stream",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "new-package": "node scripts/create-package.js"
  },
  "keywords": [
    "monorepo",
    "fast-tracker"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
